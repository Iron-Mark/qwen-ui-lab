/** Shared horizontal layout for header, footer, and page content gutters. */
export const PAGE_MAX_WIDTH_CLASS = "max-w-[96rem]";

export const PAGE_GUTTER_CLASS = "px-4 sm:px-6 lg:px-8";

export const PAGE_CONTAINER_CLASS = `mx-auto ${PAGE_MAX_WIDTH_CLASS} ${PAGE_GUTTER_CLASS}`;
